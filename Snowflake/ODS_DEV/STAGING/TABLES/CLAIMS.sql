create or replace TABLE ODS_DEV.STAGING.CLAIMS (
	CLAIM_ID NUMBER(38,0) NOT NULL,
	POLICY_NUM NUMBER(38,0),
	CLAIM_TYPE VARCHAR(16777216),
	CLAIM_DATE VARCHAR(16777216),
	CLAIM_STATUS VARCHAR(16777216),
	primary key (CLAIM_ID)
);
